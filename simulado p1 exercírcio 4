#4 -Faça um programa em python que verifique a letra digitada pelo usuário. Se a letra
#digitada for uma consoante imprima a mensagem “Você digitou uma consoante”.
#Se a letra digitada for uma vogal, imprima “Você digitou uma vogal”. (16p)


def main(args):
	L = input("Digite uma letra: ")
	if(L== "a" or L== "e" or L== "i" or L== "o" or L== "u"):
		print ("Você digitou uma vogal")
	else:
		print("Você digitou uma consoante")
	return 0

if __name__ == '__main__':
	import sys
	sys.exit(main(sys.argv))
